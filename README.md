# Sequence-detector
**Sequence Detector:** A sequence detector is a **sequential logic circuit** that detects a specific pattern or sequence of binary bits in an incoming data stream. It produces an output when the required sequence is detected.  **Example:** A detector for `1011` gives output **1** whenever `1011` occurs.
